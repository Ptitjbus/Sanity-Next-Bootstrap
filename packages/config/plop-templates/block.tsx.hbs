import {Block} from '@local/ui/blocks' export interface
{{pascalCase name}}Props { className?: string } export const
{{pascalCase name}}
= ({className, ...rest}:
{{pascalCase name}}Props) => { return (
<Block className='{className}' {...rest}>
  {/* */}
</Block>
) }